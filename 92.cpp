#include <iostream>

using namespace std;

int main() {
	int n,a[n],sum=0,i;
	cin>>n;
	for(i=0;i<n;i++)
{
    cin>>a[i];
}
    for(i=0;i<=a[i];++i)
    {
        sum=sum+a[i];
    }
	cout<<sum;

   if(!cin)
    {
        cout<<"Invalid input";
    }
   
}
